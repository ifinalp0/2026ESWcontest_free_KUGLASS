{
  "name": "kuglass-tabui",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "KUGLASS tablet UI and ESP32_A gateway.",
  "scripts": {
    "setup:python": "python3 -m venv .venv && .venv/bin/python -m pip install -r requirements.txt",
    "dev": "vite --host 0.0.0.0",
    "dev:api": ".venv/bin/python server.py --host 0.0.0.0 --port 8080 --transport usb --usb-port auto",
    "start": ".venv/bin/python server.py --host 0.0.0.0 --port 8080 --transport usb --usb-port auto",
    "start:open": "sh -c '(sleep 1; open http://127.0.0.1:8080/demo) & exec .venv/bin/python server.py --host 0.0.0.0 --port 8080 --transport usb --usb-port auto --hil'",
    "build": "tsc --noEmit --incremental false && vite build",
    "typecheck": "tsc --noEmit --incremental false",
    "check:python": "PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest discover -s tests -v",
    "check": "npm run typecheck && npm run check:python"
  },
  "dependencies": {
    "@react-three/fiber": "^9.1.2",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@types/three": "^0.171.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
