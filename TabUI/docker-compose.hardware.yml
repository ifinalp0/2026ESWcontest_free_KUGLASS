services:
  tabui:
    environment:
      TABUI_TRANSPORT: serial
      TABUI_SERIAL_PORT: /dev/ttyUSB0
      TABUI_HIL_ENABLED: "${TABUI_HIL_ENABLED:-0}"
    devices:
      - "${TABUI_SERIAL_DEVICE:-/dev/ttyUSB0}:/dev/ttyUSB0"
    group_add:
      - "${TABUI_DIALOUT_GID:-20}"
